import styled from "styled-components";

export const JordanHeader = styled.div`
    display: flex;
    justify-content: center;
    align-items: center;
    text-align: center;
    padding: 1.5rem 0 1.6rem 0;

    h2 {
        font-size: 2.4rem;
        font-weight: 600;
        color: var(--Black-Light);
    }
`